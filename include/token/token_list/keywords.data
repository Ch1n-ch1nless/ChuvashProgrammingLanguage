#define KEYWORDS \
    EMPTY_TOKEN(Function, "func") \
    EMPTY_TOKEN(Return, "ret") \
    EMPTY_TOKEN(If, "if") \
    EMPTY_TOKEN(Else, "else") \
    EMPTY_TOKEN(Cycle, "while") \
    EMPTY_TOKEN_LAST(Var, "var") \
